```{=html}
<section class="products" id="products">
  <div class="container">
    <div class="products-content">
      <h2>Featured Products</h2>
      <div class="product-grid">
        <% for (const item of items) { %>
          <% for (const product of item.products) { %>
            <div class="product"
              data-name="<%- product.name %>" 
              data-price="<%- product.price %>" 
              data-discounted-price="<%- product.discounted_price %>" 
              data-discount-percent="<%- product.discount_percent %>" 
              data-payment-options="<%- product.payment_options %>"
              data-sizes="<%- product.sizes %>"
              data-description="<%- product.description %>" 
              data-images="<%- product.images %>">
              <!-- Use only the first URL from the comma-separated list for the grid -->
              <img src="assets/products/<%- product.images.split(',')[0].trim() %>" alt="<%- product.name %>">
              <div class="product-info">
                <h3 class="product-name"><%- product.name %></h3>
                <p class="product-price">₹<%- product.price %></p>
              </div>
            </div>
          <% } %>
        <% } %>
      </div>
    </div>
  </div>
</section>
```